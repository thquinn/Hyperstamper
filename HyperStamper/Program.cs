using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace HyperStamper
{
    class Program
    {
        static void Main(string[] args)
        {
            // Initialize our PartsInfo with the desired product.
            PartsInfo info = new PartsInfo(PartsInfo.DOCKING_CLAMP);

            // TODO: Create a hashable collection of parts.

            // TODO: Do an iterative search on the collection of parts to see if it can be combined into the product.
        }
    }
}
